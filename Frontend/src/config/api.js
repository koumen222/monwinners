// URL du backend - pas besoin de clé API dans le frontend
// Le backend gère tous les appels API externes (OpenAI, etc.)
// Note: Le port doit correspondre au port sur lequel le serveur backend démarre
const API_BASE_URL = 'http://localhost:3000/api';

export default API_BASE_URL;

